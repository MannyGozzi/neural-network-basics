class NeuralNetwork {
  
    constructor(num_inputs, num_hidden, num_outputs) {
      this.num_inputs  = num_inputs;
      this.num_hidden  = num_hidden;
      this.num_outputs = num_outputs;
    }
    
    
  }